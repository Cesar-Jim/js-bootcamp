// ************************************************************ MORE ON VARIABLES

// 1. You can't define a variable more than once
let petName = 'Hal';
petName = 'Spot';

console.log(petName);

// 2. There are rules related to variable names
// variables can start with letters or the special characters: $ 'or' _
let _3 = 2;
let result = _3 + 4;

console.log(result);

// 3. Variable names cannot be reserved keywords
// let let = 2
